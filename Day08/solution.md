### Solution

To perform this task we can use the below command to install ansible version 4.9.0 on jump host.

```
sudo pip3 install ansible==4.9.0
```

To verify the installation use below

```
ansible --version
```
